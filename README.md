# hsl26-robot-b-detector

Решение квалификационного задания хакатона StarLine 2026 (HSL26): детекция и
локализация мобильного робота Turtlebot2 ("робот Б") в облаках точек лидара
Livox MID-360, записанных роботом "А" на ROS 2 Humble. Регламент задания
описан в `docs/regulations.md`.

Проект сейчас на самом старте: собран только скелет ROS 2 пакета, алгоритм
детекции ещё не реализован.

Требования: ROS 2 Humble, Python 3.10+, зависимости `numpy` и `mcap-ros2-support`.

Установка:

```
mkdir -p ~/ros2_ws/src
cd ~/ros2_ws/src
git clone git@github.com:Hrand29/hsl26-robot-b-detector.git
cd ~/ros2_ws
colcon build --symlink-install
source install/setup.bash
```

Данные: bag-файлы (7 записей — static_1m, rot_0.5m/1m/2m/3m, mov_01/02) в
репозиторий не включены из-за размера. Скачать по ссылке из регламента
квалификации HSL26 и распаковать в папку `data/`.

Запуск: инструкция появится по мере готовности детектора.

Видео демонстрации: ссылка появится перед сдачей задания.

Roadmap:
- Офлайн-чтение `/livox/lidar` из mcap-файлов
- Удаление плоскости пола
- Кластеризация точек, выделение кандидата "робот Б"
- Оценка позы (x, y, при возможности yaw)
- Трекинг между кадрами для сценариев mov_01/mov_02
- Визуализация в RViz и запись демо-видео
